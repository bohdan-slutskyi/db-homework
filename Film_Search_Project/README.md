# Film Search Project

Веб-приложение на Python для поиска фильмов в базе данных **MySQL Sakila**.
Проект показывает совместное использование реляционной базы данных MySQL для
каталога фильмов и документной базы MongoDB для истории поисковых запросов и
статистики.

---

## Описание проекта

Приложение позволяет искать фильмы по части названия, жанру и диапазону лет.
Результаты отображаются постранично. Каждый реальный поисковый запрос
сохраняется в MongoDB, а на главной странице доступны последние уникальные и
популярные запросы.

---

## Функциональные возможности

- Поиск по части названия фильма.
- Поиск по жанру и году или диапазону лет.
- Дополнительные фильтры: актёр, возрастной рейтинг, стоимость аренды и
  длительность фильма.
- Пагинация: не более десяти фильмов на странице.
- Сортировка каталога по году выпуска.
- Отдельная страница с подробностями фильма.
- Сохранение поисковых запросов в MongoDB.
- Статистика: пять последних уникальных и пять популярных запросов.
- Локальные файлы логов: поисковые события и ошибки.
- Корректные ответы при недоступности MySQL или MongoDB.

---

## Технологический стек

- **Python 3.12** — серверная логика приложения.
- **FastAPI** — обработка HTTP-запросов и маршруты сайта и JSON API.
- **Jinja2** — передача данных из Python в HTML-страницы.
- **Tailwind CSS** — оформление и адаптивная компоновка HTML-страниц.
- **MySQL Sakila** — каталог фильмов, жанры, актёры и годы выпуска.
- **MongoDB** — история поисков и статистика запросов.
- **mysql-connector-python** — драйвер подключения к MySQL.
- **PyMongo** — драйвер подключения к MongoDB.
- **Uvicorn** — ASGI-сервер FastAPI.
- **uv** — управление зависимостями Python.
- **Dockerfile и Compose** — контейнерный запуск приложения и MongoDB.

---

## Структура проекта

```text
Film_Search_Project/
├── app/
│   ├── main.py                  # FastAPI-маршруты: HTML и JSON API
│   ├── search_context.py        # Подготовка данных главной страницы и статистики
│   ├── search_service.py        # Общая логика поиска по названию и фильтрам
│   ├── search_helpers.py        # Проверка и подготовка параметров поиска
│   ├── mysql_connector.py       # SQL-запросы к MySQL Sakila
│   ├── mongo_logger.py          # Запись поисков и MongoDB-агрегации
│   ├── logging_config.py        # Настройка файлов search.log и error.log
│   └── catalog_presentation.py  # Подготовка данных каталога для отображения
├── templates/
│   ├── base.html                 # Общий каркас, Tailwind CSS и переключение темы
│   ├── index.html                # Поиск, каталог, фильтры и статистика
│   ├── results.html              # Отдельная страница результатов
│   ├── film_detail.html          # Подробности фильма
│   └── error.html                # Безопасное сообщение об ошибке MySQL
├── logs/                        # Локальные логи работы приложения
├── compose.yml                  # Совместный запуск приложения и MongoDB
├── Dockerfile                   # Сборка контейнера приложения
├── pyproject.toml               # Зависимости Python
├── uv.lock                      # Зафиксированные версии зависимостей
├── .env.example                 # Образец параметров MySQL
├── smoke_test.sh                # Быстрая проверка ключевых маршрутов
└── README.md                    # Описание и запуск проекта
```

---

## Используемые данные

### MySQL Sakila

MySQL хранит данные каталога:

- `film` — название, год выпуска, описание, рейтинг, стоимость аренды и
  длительность;
- `category` — жанры;
- `film_category` — связь фильмов и жанров;
- `actor` и `film_actor` — актёры и их связь с фильмами.

### MongoDB

В MongoDB создаётся именованная коллекция поисковых логов. Каждый документ
содержит:

- `timestamp` — время выполнения поиска;
- `search_type` — сценарий поиска: по названию или по фильтрам;
- `params` — параметры поиска;
- `results_count` — полное число найденных фильмов.

Последние запросы строятся как уникальные сочетания `search_type` и `params`.
Популярные запросы строятся MongoDB-агрегацией и показывают число повторений.

---

## Технические особенности

- SQL-запросы используют параметры, а не подстановку пользовательского ввода в
  текст SQL.
- Один поиск создаёт один документ в MongoDB; переход по страницам выдачи не
  создаёт повторных событий.
- Ошибка MongoDB не мешает поиску фильмов в MySQL: запись лога или статистика
  безопасно пропускаются.
- Ошибка MySQL возвращает безопасный ответ без параметров подключения.
- Uvicorn запускает приложение как `app.main:app`, поэтому отдельный блок
  `if __name__ == "__main__":` не требуется.

---

## Настройка и запуск

### Предварительные условия

- Podman с Compose. Для Ubuntu: [официальная инструкция по установке Podman](https://podman.io/docs/installation).
- Или Docker Engine с Docker Compose Plugin. Для Ubuntu: [официальная инструкция по установке Docker](https://docs.docker.com/engine/install/ubuntu/).
- Доступ к MySQL-серверу с базой данных Sakila.

### Локальные параметры MySQL

Рядом с `compose.yml` создаётся локальный файл `.env` на основе
`.env.example`:

```dotenv
MYSQL_HOST=your_mysql_host
MYSQL_PORT=3306
MYSQL_USER=your_mysql_user
MYSQL_PASSWORD=your_mysql_password
MYSQL_DATABASE=sakila
```

MongoDB запускается Compose как отдельный внутренний сервис, поэтому параметры
подключения к нему для обычного запуска добавлять в `.env` не требуется.

### Запуск

```bash
podman compose -f compose.yml up --build -d app
```

При использовании Docker Compose применяется аналогичная команда:

```bash
docker compose -f compose.yml up --build -d app
```

После запуска сайт доступен по адресу:

```text
http://localhost:5001/
```

---

## Проверка

Быстрая проверка главной страницы, healthcheck, JSON-поисков и статистики:

```bash
./smoke_test.sh
```

Ожидаемый итог:

```text
Smoke-тесты пройдены.
```
